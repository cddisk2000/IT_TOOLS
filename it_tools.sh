#!/bin/bash

while :
do
    echo "
    +----------------------------------------------------------------------
    | 2025.03.27 Write By Landy.Wang v1.0
    | Blog http://my-fish-it.blogspot.com
    +----------------------------------------------------------------------
    | Install Agent Gemini Cli  1
    +----------------------------------------------------------------------
    | Install Agent Copilot Cli 2
    +----------------------------------------------------------------------
    | Install Agent Codex Cli   3
    +----------------------------------------------------------------------
    | Exit                      4
    +----------------------------------------------------------------------
    "
    
	#关闭 Linux 安全限制
    sed -i 's/SELINUX=enforcing/SELINUX=disabled/' /etc/selinux/config
    setenforce 0

    read -rp "Please Choice " select
#===============================================
    if [[ $select == 1 ]]; then

    fi
#=============================================
	if [[ $select == 2 ]]; then
	
	
    fi
#=============================================
    if [[ $select == 3 ]]; then

    fi	
#=============================================
#=============================================	
    if [[ $select == 4 ]]; then
         echo "Exiting..."
         break
    fi
#=============================================		 
done
